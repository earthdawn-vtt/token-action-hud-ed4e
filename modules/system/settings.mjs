/**
 * Register all module settings with Foundry.
 * @param {function} onChangeFunction This function can be called in the registered settings onChange parameter
 * to trigger Token Action HUD Core's own settings update process when a TAH system setting is updated.
 */
export function register( onChangeFunction ) {

}